"""Foreign application config."""

from django.apps import AppConfig


class ForeignConfig(AppConfig):
    """Foreign application config."""

    name = 'foreign'
    verbose_name = 'Иностранный язык'
