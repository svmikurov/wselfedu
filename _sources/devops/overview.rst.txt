The application uses `Docker <https://www.docker.com/>`_

For start and stop application use :ref:`commands <run_stop_app>`.

All docker files are in ``./docker``.

The docker compose files are in the root.