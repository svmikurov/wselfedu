[project]
name = "wse"
version = "0.1.0"
description = ""
authors = [
    {name = "Sergei Mikurov",email = "454004@mail.ru"}
]
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
]

[tool.poetry]
packages = [{include = "wse", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
domain = [
]
application = [
    "dependency-injector (>=4.49.0,<5.0.0)"
]
infrastructure = [
]
entrypoints = [
    "flask (>=3.1.3,<4.0.0)"
]
dev = [
    "pytest (>=9.0.3,<10.0.0)",
    "ruff (>=0.15.15,<0.16.0)",
    "mypy (>=2.1.0,<3.0.0)"
]
docs = [
    "sphinx (>=9.1.0,<10.0.0)"
]
